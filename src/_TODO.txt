move anim select dropdown to window header??
exponential hsv value relationship with slider to drop brightness off faster
spice up UI with BG images, button/slider shapes, icons
double check slider dragging feedback
proper setup of anim selector - list from rcvr?
is esp_now data recv interrupt based or sync'd with loop?
    possible issues with data buffering critical section
rcvr & xmit - queue send & recv cmds for packet aggregation in loop
transfer sound files by esp now

need luminance control over generated colors to keep them from being too bright
